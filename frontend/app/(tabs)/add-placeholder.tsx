import React from "react";
import { View } from "react-native";

// Placeholder route backing the centre "Add" tab button.
// The button itself intercepts presses and opens the /add modal instead.
export default function AddPlaceholder() {
  return <View />;
}
